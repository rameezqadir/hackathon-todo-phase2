name: Deploy Frontend
on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'vercel.json'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Deploy status
        run: echo "Frontend should be deployed separately to Vercel"
